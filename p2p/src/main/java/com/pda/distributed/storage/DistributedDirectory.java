package com.pda.distributed.storage;

// Sabe dónde está cada archivo en la red P2P (como un índice)
public class DistributedDirectory {

    public DistributedDirectory() {
        // Inicialización
    }
}
